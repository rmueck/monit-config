CHECK PROGRAM seafile WITH PATH /etc/monit/scripts/check_container_seafile.sh
  START PROGRAM = "/etc/monit/scripts/start_seafile.sh"
  STOP PROGRAM = "/etc/monit/scripts/stop_seafile.sh"
#  IF status != 0 FOR 2 CYCLES THEN RESTART
  IF status != 0 FOR 2 CYCLES THEN alert
#  IF 2 RESTARTS WITHIN 3 CYCLES THEN UNMONITOR
