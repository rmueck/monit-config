CHECK PROGRAM hedgedoc WITH PATH /etc/monit/scripts/check_container_hedgedoc.sh
  START PROGRAM = "/etc/monit/scripts/start_hedgedoc.sh"
  STOP PROGRAM = "/etc/monit/scripts/stop_hedgedoc.sh"
  IF status != 0 FOR 1 CYCLES THEN RESTART
  IF status != 0 FOR 2 CYCLES THEN alert
#  IF 2 RESTARTS WITHIN 3 CYCLES THEN UNMONITOR
